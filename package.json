{
  "name": "planotterpoc",
  "version": "0.1.0",
  "engines": {
    "node": "10.12.0",
    "yarn": "1.12.3"
  },
  "scripts": {
    "start": "node ./back/dist/index.js",
    "postinstall": "yarn install --cwd back && yarn install --cwd front && yarn run buildSubs",
    "buildSubs": "yarn run --cwd back build && yarn run build --cwd front"
  },
  "license": "ISC"
}
