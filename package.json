{
  "name": "planotterpoc",
  "version": "0.1.0",
  "engines": {
    "node": "10.12.0",
    "yarn": "1.12.3"
  },
  "scripts": {
    "prepareSubs": "yarn install --cwd back && yarn install --cwd front && yarn run buildSubs",
    "buildSubs": "npm run build --prefix back && npm run build --prefix front"
  }
}
