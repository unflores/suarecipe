{
  "name": "planotterpoc",
  "version": "0.1.0",
  "engines": {
    "node": "10.12.0",
    "npm": "6.4.1"
  },
  "scripts": {
    "postinstall": "npm run installSubs && npm run buildSubs",
    "installSubs": "npm install --prefix back && npm install --prefix front",
    "buildSubs": "npm run build --prefix back && npm run build --prefix front"
  }
}
