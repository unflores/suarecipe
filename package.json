{
  "name": "planotterpoc",
  "version": "0.1.0",
  "engines": {
    "node": "10.12.0",
    "yarn": "1.19.1"
  },
  "scripts": {
    "start": "node ./back/dist/index.js",
    "postinstall": "yarn install --cwd back && yarn install --cwd front && yarn run buildSubs",
    "buildSubs": "cd ./back ; yarn run build:production; cd ../ && cd front; yarn run build:production; cd ../"
  },
  "license": "ISC",
  "devDependencies": {
    "typings-for-css-modules-loader": "^1.7.0"
  }
}
